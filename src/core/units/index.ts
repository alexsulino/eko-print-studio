/**
 * Units barrel — canonical entry for length conversion.
 * `src/core/document/units.ts` re-exports these symbols for backward compatibility.
 */
export {
  UnitsEngine,
  toPixels,
  fromPixels,
  getDocumentPixelSize,
} from './UnitsEngine'
